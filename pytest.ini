[pytest]
# Sem isso, "pytest -v" (chamado diretamente, sem o "python -m") não coloca
# a raiz do projeto no sys.path, e o import "from src.device import ..."
# falha com ModuleNotFoundError: No module named 'src'.
pythonpath = .
testpaths = tests
